import React from 'react';
import ComingSoon from '../../../components/ComingSoon';

// TODO Phase 2 : portage de frontend/src/pages/ProfileData.tsx
// (export RGPD via expo-file-system + expo-sharing, suppression de compte).
export default function ProfileData() {
  return (
    <ComingSoon title="Mes données">
      L'export de vos données et la suppression du compte se font pour l'instant depuis la version
      web.
    </ComingSoon>
  );
}
